image: archlinux/base:latest

before_script:
    - pacman --noconfirm -Syu
    - pacman --noconfirm -S grep gcc clang make

stages:
    - build
    - test

gcc:
    stage: build
    script:
    - export CXX=g++
    - make -j$(nproc)
    artifacts:
        paths:
        - expected_test

clang:
    stage: build
    script:
    - export CXX=clang++
    - make -j$(nproc)
    - ci/verify

unit_tests:
    stage: test
    script:
    - ci/verify
